数据类型：

基本类型：char short int float double long
构造类型：一维数组  二维数组  指针数组   函数指针数组
指针类型：一级指针  二级指针  数组指针   函数指针
空类型  ：void

空指针：NULL  (void *)0

vim -t 类型名
man
ctrl + ]


常量：程序运行期间数据不发生变化的数据

整型常量：int
浮点常量：
指数常量：[+|-]m.n e|E [+|-]T
标识常量：
字符常量：‘’
字符串常量：“”

0 '\0' '0' "0"

/
%
++    a = a ++ + ++b
--

> >= < <= == !=

if(x = 5) //赋值

if(5 == x) //等于

!  &&  ||

~
&        &&
|        ||
^

交换变量值:
a = a ^ b;
b = a ^ b;
a = a ^ b;

,

sizeof();

int a;
char b;
char a[5];
char *a;
int * p[3];

strlen();
